name: reset

# One-time, run manually with the other workflows paused: cancels pending runs,
# closes all open issues and agent PRs, clears the board, resets the wiki's
# memory pages, and deletes the previous product from main.
# Safe to delete after running.
on:
  workflow_dispatch:

# Wider than the rest of the pipeline because this job is the one that dismantles
# it: contents to delete the old product from main, actions to cancel the runs
# that would otherwise race the reset, pull-requests to close abandoned agent PRs.
permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write

jobs:
  reset:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          # The PAT, not GITHUB_TOKEN: the checkout's credentials are what pushes
          # the product deletion to main, and a push by GITHUB_TOKEN would not
          # carry the permissions the rest of the pipeline was built around.
          token: ${{ secrets.AGENT_PAT }}

      - uses: ./.github/actions/setup

      - name: Reset GitHub state
        run: node agents/reset.mjs
        env:
          GH_TOKEN: ${{ secrets.AGENT_PAT }}
          GH_PROJECT_OWNER: '@me'
          GH_PROJECT_NUMBER: '3'
