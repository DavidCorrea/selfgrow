/**
 * garden.js — selfgrow garden logic
 *
 * Exports initGarden(scene) which is called once the Three.js scene
 * is set up. Future growth (plants, weather, creatures) should be
 * added here.
 */
export function initGarden(scene) {
  // The foundation scene is already set up in index.html.
  // This hook is reserved for future botanical life, weather, and
  // creature behaviour to be added here as the garden grows.

  // For now: log that the garden is ready.
  console.log('selfgrow garden initialised. The soil awaits…');
}
